\documentclass{article}
\usepackage{amsmath}% for equation environment
\usepackage{mathtools}% for xRightarrow
\begin{document}
\section*{Interacting Multipoles of Rank 2 and 3:}
\begin{equation}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{equation}


\section*{Simplify deltas}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Simplify R}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Simplify Multipoles}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Exploit Traceless Conditions}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Reduce Indices}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Add up}
\begin{equation}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = R_{z}^{-11} \cdot \frac{1}{105} \cdot \left[ + 0 \right]
\end{equation}


\section*{Combining everything}
\begin{equation}
T_{\alpha{}\beta{}\gamma{}\delta{}\epsilon{}} \Theta{}_{\alpha{}\beta{}} \Omega{}_{\gamma{}\delta{}\epsilon{}} = \left[ + 0 \right]
\end{equation}


\end{document}