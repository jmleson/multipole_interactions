\documentclass{article}
\usepackage{amsmath}% for equation environment
\usepackage{mathtools}% for xRightarrow
\begin{document}
\section*{Interacting Multipoles of Rank 2 and 2:}
\begin{equation}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{equation}


\section*{Simplify deltas}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Simplify R}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Simplify Multipoles}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Exploit Traceless Conditions}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Reduce Indices}
\begin{subequations}\begin{gather}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\\ \xRightarrow{\text{cleaned up:}}
 R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{gather}\end{subequations}


\section*{Add up}
\begin{equation}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = R_{z}^{-9} \cdot - \frac{1}{15} \cdot \left[ + 0 \right]
\end{equation}


\section*{Combining everything}
\begin{equation}
T_{\alpha{}\beta{}\gamma{}\delta{}} \Theta{}_{\alpha{}\beta{}} \Theta{}_{\gamma{}\delta{}} = \left[ + 0 \right]
\end{equation}


\end{document}